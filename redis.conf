# Disable Redis from being accessed outside localhost
bind 0.0.0.0

# Disable protected mode (note: ensure Redis is not exposed to public internet)
protected-mode no

# Log level for potential issues
loglevel notice

# Only accept authenticated connections
requirepass admin
